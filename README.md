# 1. Fixed-Capacity Vector (No Dynamic Allocation)
Skills: memory management, templates, iterators, deterministic behaviorDescription:Implement a FixedVector<T, N> that behaves like std::vector but never allocates.
* Enforce bounds
* Provide iterators
* Provide push_back, erase, insert
* Optional: custom allocator and placement new